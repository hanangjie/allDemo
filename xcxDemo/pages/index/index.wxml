<!--index.wxml-->
<view class="container">
  <view class="usermotto">
    <text class="user-motto">{{motto}}{{userInfo.nickName}}{{model}}</text>
  </view>
    <button  bindtap="bindViewTap">上传照片</button>
    <image src="{{temp}}"></image>
</view>
